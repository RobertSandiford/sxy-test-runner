import { describe } from '../../dist/index.js'
import { expect, should } from 'chai'
should()

describe('Sample test 2', ({it}) => {

    it('2 should equal 2', () => {
        expect(2).to.equal(2)
    })

})